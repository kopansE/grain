import { Placeholder } from '@/components/ui/Placeholder';

export default function CommandCenter() {
  return <Placeholder title="Command Center" phase={4} blurb="The globe, the KPIs, what's next, and the relationship radar." />;
}
